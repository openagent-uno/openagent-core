[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "openagent-gateway"
version = "1.0.0b1"
requires-python = ">=3.11"
dependencies = ["openagent-core==1.0.0b1", "aiohttp>=3.10,<4"]
[tool.setuptools.packages.find]
where = ["src"]
