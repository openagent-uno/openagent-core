[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "openagent-storage-sqlite"
version = "1.0.0b1"
requires-python = ">=3.11"
dependencies = ["openagent-core==1.0.0b1"]
[tool.setuptools.packages.find]
where = ["src"]
